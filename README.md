# Nuxt 3 Minimal SSD Conference

## Setup

```sh

npm install
npm run dev

# edit data.json and refresh the page
```

## build

```sh
npm install
npm run generate
npx nuxt build --preset github_pages
# put .output/public on a static hosting service
```
